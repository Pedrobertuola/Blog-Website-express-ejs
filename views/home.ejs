<%- include('partials/header'); -%>

<h1>Home</h1>

    <p><%= startingContent %></p>
     <% console.log(posts); %> 

    <% posts.forEach(function(post){ %>
    <%  const reduceContent =  post.content.substring(0,100)       %>
      <h1> <%=post.title%>   </h1> 
      <p> <%=reduceContent + "..."%> 
      
       <a href="/posts/<%= post.title%>">Read More</a>   
      </p>  
    <% }) %>      




<%- include('partials/footer'); -%>


